{% extends 'base.html.twig' %}

{% block title %}Author{% endblock %}

{% block body %}
    <h1>Author</h1>
    <div class="row mb-3">
        <div class="col-md-4">
            <a class="btn btn-success" href="{{ path('app_author_edit', {'id': author.id}) }}">edit</a>
        </div>
        <div class="col-md-4">
            <a class="btn btn-secondary" href="{{ path('app_author_index') }}">back to list</a>
        </div>
        <div class="col-md-4">
            {{ include('author/_delete_form.html.twig') }}
        </div>
    </div>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ author.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ author.name }}</td>
            </tr>
        </tbody>
    </table>

{% endblock %}
